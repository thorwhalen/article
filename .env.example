# article — configuration & secrets (copy to `.env`; never commit the real .env)
# Loaded by article.config.Settings via pydantic-settings.
# Every platform is optional: omit a platform's secrets to skip it.

# --- Substack (Phase 1, primary) ---------------------------------------------
# Public URL of your publication; the canonical SSOT URL is derived from it.
SUBSTACK_PUBLICATION_URL=https://yourname.substack.com
# Credentials are only needed when no saved browser session exists yet.
SUBSTACK_EMAIL=you@example.com
SUBSTACK_PASSWORD=
# Where the Playwright storage_state (cookies/session) is persisted/reused.
SUBSTACK_SESSION_PATH=.article/substack_session.json

# --- Medium (Phase 2) --------------------------------------------------------
# Self-issued integration token: Settings > Security and apps > Integration tokens.
# NOTE: Medium's API is deprecated but still works with a self-issued token.
MEDIUM_TOKEN=
MEDIUM_USER_ID=

# --- Dev.to / Forem (Phase 2) ------------------------------------------------
# Generate at https://dev.to/settings/extensions ("DEV API Keys").
DEVTO_API_KEY=

# --- Hashnode (Phase 2) ------------------------------------------------------
# Personal Access Token from https://hashnode.com/settings/developer
HASHNODE_TOKEN=
# Publication ObjectId (query `publication(host: "...") { id }`).
HASHNODE_PUBLICATION_ID=

# --- Pipeline ----------------------------------------------------------------
# JSON state store; the canonical_url SSOT lives here, keyed by article slug.
STATE_PATH=pipeline_state.json
# Human-like pacing window (seconds) for browser automation.
MIN_ACTION_DELAY=0.6
MAX_ACTION_DELAY=2.4
